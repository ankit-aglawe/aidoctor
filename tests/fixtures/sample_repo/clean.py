"""Clean file with no violations. Used to verify clean files don't generate false positives."""


def add(a: int, b: int) -> int:
    """Add two integers."""
    return a + b


def greet(name: str) -> str:
    """Greet a person."""
    return f"Hello, {name}!"
