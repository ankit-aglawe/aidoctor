"""aidoctor engine — internal modules for scanning, rendering, and orchestration.

Not part of the public API. Workflow skills and external consumers should
import from the top-level `aidoctor` package only.
"""
