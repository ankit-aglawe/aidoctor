"""Billing module skeleton.

Functions are declared with signatures but not yet implemented. They raise
NotImplementedError so any accidental call fails loudly at runtime instead of
shipping as a silent no-op.
"""


def charge_card(amount: int) -> None:
    raise NotImplementedError(
        "charge_card pending PSP integration (next sprint)"
    )


def refund_card(charge_id: str) -> None:
    raise NotImplementedError(
        "refund_card pending PSP integration (next sprint)"
    )
